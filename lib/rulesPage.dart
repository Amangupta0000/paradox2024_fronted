import 'package:flutter/material.dart';

class rulesPage extends StatelessWidget {
  const rulesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This is the Rules Page'),
      ),
    );
  }
}
